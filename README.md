# Air-Writing-Console
Air Writing Recognition System using Arduino and ML. Captures 6-axis motion data (MPU6050) and classifies air-drawn letters in real time using a Random Forest model. Includes data preprocessing, feature extraction, and live prediction pipeline with ~90–95% accuracy.
